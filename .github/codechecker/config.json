{
  "analyze": [
    "--enable=default",
    "--enable=portability",
    "--enable=security",
    "--enable=sensitive",
    "--disable=google",
    "--skip=.github/codechecker/skipfile.txt"
  ],
  "parse": [
    "--skip=.github/codechecker/skipfile.txt"
  ]
}
