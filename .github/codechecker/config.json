{
  "analyze": [
    "--enable=default",
    "--enable=portability",
    "--enable=security",
    "--enable=sensitive",
    "--analyzer-config=clang-tidy:take-config-from-directory=true",
    "--skip=.github/codechecker/skipfile.txt"
  ],
  "parse": [
    "--skip=.github/codechecker/skipfile.txt"
  ]
}
